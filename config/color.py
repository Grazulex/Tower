# Color definitions used in the game.

BLACK = (0, 0, 0)  # Represents the color black (RGB format).
WHITE = (200, 200, 200)  # Represents the color white (RGB format).
GREEN = (0, 255, 0)  # Represents the color green (RGB format).
RED = (255, 0, 0)  # Represents the color red (RGB format).
GRAY = (100, 100, 100)  # Represents the color gray (RGB format).
DARK_GRAY = (50, 50, 50)  # Represents a darker shade of gray (RGB format).
YELLOW = (255, 255, 0)  # Represents the color yellow (RGB format).
BLUE = (0, 0, 255)  # Represents the color blue (RGB format).